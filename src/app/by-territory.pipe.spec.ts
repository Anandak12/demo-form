import { ByTerritoryPipe } from './by-territory.pipe';

describe('ByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ByTerritoryPipe();
    expect(pipe).toBeTruthy();
  });
});
