import { Ng2OrderByPipe } from './ng2-order-by.pipe';

describe('Ng2OrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new Ng2OrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
